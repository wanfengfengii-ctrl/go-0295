# 高层公建岩棉外保温抹面移交质量闭环

## 项目目标

构建一个可编译运行的 Go HTTP 后端，围绕单一施工闭环管理高层公共建筑各楼栋立面分区从基层交接到允许施工抹面胶浆的全过程。系统在任务锁定时固化构造、材料、网格、阈值、邻接关系、设备调用策略和抽检映射；施工期间以只追加的“立面分区—铺贴网格—岩棉板代次”覆盖图、逐板连续布胶覆盖集合、整数克材料守恒池、限时互斥设备租约和逻辑时间证据链推进状态。异常按锁定关系确定传播范围，返工仅能创建新代次覆盖旧结论且完整保留历史，最终由单写屏障在移交、质量隔离和取消之间作出唯一裁定。生产实现控制在20—30个Go文件、2000—3000有效生产代码行，目标约2500行，划分为至少4个有实际职责的包；使用嵌入式持久化数据库和事务日志支持崩溃恢复，提供go.mod及linux/amd64、linux/arm64双架构Docker构建。

## 端到端业务流程

1. 任务建档与锁定：录入楼栋、立面分区、基层墙体类型和共享测区，导入整数毫米网格、洞口、翻包范围、防火隔离带、板片邻接表及禁区；绑定岩棉板批次与逐板编号、密度和导热系数证明摘要、粘结砂浆批次、布胶方案、锚栓型号与孔位、养护日程、抽检映射和全部阈值，校验摘要一致后生成不可变规则快照及任务代次。
2. 基层与砂浆准备：设备适配器按逻辑时间采集基层测点；合格后，操作员以幂等操作号申请搅拌机和电子秤租约，在同一事务中领取整数克干粉与用水、登记留样及合理损耗，校验配水比并形成有开放截止时间的砂浆拌合代次；失败必须同时回滚材料和租约。
3. 逐板施工：按锁定网格顺序为每块板获取布胶工位租约，记录布胶分区的连续有效覆盖集合和板面粘结质量，满足面积率后才能铺贴；铺贴校验批次、板号、网格覆盖、错缝、洞口角部禁区、翻包及隔离带关系，随后处理板缝。每块板及其锚栓只能追加完整工序前缀，钻孔设备租约有效时才可按序记录孔位、孔距、边距和有效锚固深度。
4. 养护与检测：温湿度探头按逻辑时间写入连续区间，使用整数定点积分计算养护等效时长；达到锁定条件后，依据抽检映射租用拉拔仪和检测通道，记录锚栓拉拔及粘结强度。设备拒绝、断连、超时或格式错误只生成确定重试次数的待重试调用，不产生读数或推进状态。
5. 异常复验与代次覆盖：基层偏差、错批、贯通板缝、粘结不足、锚固深度不足、拉拔失败或养护中断触发一次性影响分析；以当前板片为种子，按锁定邻接图、共享基层测区和同砂浆拌合代次求并集并排序，生成唯一复验范围。返工创建新代次重新覆盖受影响板片与孔位，旧板、旧锚栓、材料消耗和证据保持可查，旧代次迟到回执被隔离。
6. 复核与终局：系统先验证覆盖完整、材料守恒、逐板布胶和锚固前缀闭合、养护连续、几何及检测达标、复验完成，再接受两名不同且具备资格的人员独立复核；满足条件后可申请抹面移交。移交、质量隔离和取消通过数据库单写终局屏障竞争，只有首个合法请求生成不可变终局记录，移交仅生成一份唯一凭据。

## 核心组件与职责

1. 保温构造与材料规则目录：维护墙体、岩棉板、砂浆、洞口翻包、防火隔离带、布胶、锚栓、养护及检测规则；生成内容寻址的不可变摘要，负责陈旧摘要、立面摘要错配和定点阈值合法性校验。
2. 立面铺贴任务及覆盖聚合：锁定立面任务、整数网格和邻接图；维护只追加板代次覆盖图、连续布胶有效覆盖集合、板缝结构及完整性投影，提供确定排序的几何冲突和影响区查询。
3. 砂浆守恒与设备租约管理器：以整数克账本管理干粉、用水、留样、有效布胶和合理损耗；通过数据库事务原子完成材料领取和设备限时互斥租约，处理续租、过期、幂等重试及并发超领。
4. 布胶铺板锚固及养护证据记录器：实现基层确认、拌浆、布胶、铺板、板缝、养护、钻孔和锚固状态机；保存逻辑时间证据链、设备调用及确定重试队列，拒绝越序和旧代次写入。
5. 拉拔复验与终局仲裁器：执行抽检映射、整数定点强度计算、异常传播、新代次复验、双人复核、资格校验及单写终局竞争，并生成可验证的抹面移交凭据。
6. Go HTTP API：提供JSON命令与查询端点、稳定错误码、幂等键和版本条件；统一事务边界、请求校验、确定性错误排序、恢复启动及健康检查。

## 领域规则与不变量

1. 所有长度使用整数毫米、质量使用整数克、比例与强度使用带明确缩放因子的有符号整数定点值；乘加、除法、舍入和时间积分必须先检查负值、除零及溢出，统一采用锁定的舍入规则，禁止浮点数参与裁定。
2. 任务锁定后，立面几何、基层测区、铺贴网格、材料批次、构造区域、布胶分区、锚栓布置、养护日程、邻接表、抽检映射、阈值和任务代次均不可原地修改；规则摘要变化只能建立新任务或返工代次。
3. 每个目标网格必须由当前有效代次的岩棉板完整覆盖且不得非法重叠；拒绝零面积或退化多边形、洞口角部禁区接缝、竖向通缝、非法错台，以及不满足锚栓板边距、孔间距或孔位干涉规则的布局。
4. 逐板状态只允许按基层确认、有效砂浆、布胶分区完整前缀、铺贴、板缝处理、养护、钻孔锚固、检测的锁定依赖追加；板号与孔位在所属代次内唯一，错误批次、开放时间耗尽、过期租约、跳步或重复提交不得产生证据。
5. 每个砂浆拌合代次必须满足“领取干粉与用水＝留样＋有效布胶＋合理损耗＋未用余量”的整数克守恒；布胶消耗须归属具体板及分区。领取材料和取得所需设备租约必须处于同一原子事务，任一失败均不得留下扣减或占用。
6. 搅拌机、布胶工位、钻孔设备、拉拔仪和检测通道实行单持有者限时租约；只有持有匹配资源、任务代次和未过期令牌的命令可提交证据。设备故障仅创建带逻辑时间、稳定故障码和确定重试上限的待重试记录。
7. 异常影响范围等于异常种子板片、锁定邻接连通规则命中的板片、共享基层测区板片以及同砂浆拌合代次板片的确定并集；同一异常事实与源代次只能生成一个复验集合。返工只能创建更高代次，历史记录不可删除，旧代次回执永不覆盖当前投影。
8. 所有命令以操作号幂等：相同操作号和规范化内容返回原结果，不同内容返回稳定幂等冲突码。错误原因按楼栋、立面分区、网格行列、板号、锚栓孔位、代次和错误码排序；终局记录采用唯一约束与比较交换语义，成功终局之后禁止任何施工或复验推进。

## 数据模型与持久化

1. RuleSnapshot：规则摘要、墙体类型、材料证明摘要、定点缩放、几何阈值、用量阈值、养护阈值、抽检映射、创建逻辑时刻。
2. FacadeTask与LockedLayout：楼栋、立面分区、任务代次、基层测区、目标网格、洞口及翻包区、防火隔离带、禁区、公开邻接边、锁定状态及当前有效代次。
3. BoardGeneration与CoverageCell：逐板编号、板材批次、网格行列、覆盖整数多边形、代次、前代记录、布胶分区区间集合、板缝、铺贴和板缝证据。
4. MortarBatch与MassLedgerEntry：砂浆批次、拌合代次、干粉和用水领取量、留样、逐板有效布胶、损耗、余量、开放截止逻辑时刻及不可变账本分录。
5. Lease与DeviceCall：资源类型和编号、持有操作、租约令牌、取得及到期逻辑时刻；设备请求摘要、尝试次数、结果状态、故障码和原始格式校验摘要。
6. AnchorGeneration与CuringEvidence：板代次、锚栓型号、序号、孔位、深度、安装证据；温湿度区间、缺口、等效时长累计值及证据链前序摘要。
7. Inspection与RetestSet：拉拔或粘结检测点、抽检映射、整数读数、裁定；异常源、确定影响成员、源代次、新代次、完成状态及迟到回执隔离记录。
8. Review、TerminalDecision与IdempotencyRecord：复核人员、资格快照、独立意见；移交、质量隔离或取消终局及唯一凭据；操作号、规范化请求摘要、响应摘要。所有实体通过嵌入式数据库事务持久化，并辅以只追加事件/账本表和启动时投影重建校验实现重启恢复。

## 公开接口

1. POST /v1/tasks与POST /v1/tasks/{id}/lock：创建并锁定立面任务；锁定返回规则与布局摘要，缺失、重叠或摘要错配返回稳定错误码及有序原因。
2. POST /v1/tasks/{id}/commands：统一提交基层采集、拌浆、布胶、铺板、板缝、养护、钻孔、锚固和检测命令；请求必须携带操作号、期望任务代次、逻辑时间及必要租约令牌。
3. POST /v1/tasks/{id}/leases/acquire和/renew：原子申请或续期设备租约；拌浆、布胶等命令可通过组合命令在同一事务中完成材料领取与租约取得。
4. POST /v1/tasks/{id}/retests与POST /v1/tasks/{id}/generations：根据既有异常事实生成唯一复验集合并开启返工代次，不接受客户端自选或删减影响范围。
5. POST /v1/tasks/{id}/reviews与POST /v1/tasks/{id}/terminal-decisions：提交独立复核以及竞争移交、隔离或取消；成功移交返回唯一凭据编号和证据摘要。
6. GET /v1/tasks/{id}、/coverage、/ledger、/evidence、/retests和/terminal：返回按领域键确定排序的当前投影与完整历史；GET /healthz报告数据库可用性和恢复校验状态。所有拒绝使用统一JSON结构{code,reasons,retryable}。

## 失败边界

1. HTTP解析、领域校验、账本写入、租约变更、覆盖投影和证据追加由单个数据库事务包围；错误或进程中断后不得留下部分材料扣减、租约、有效覆盖、锚固证据或移交凭据。
2. 启动恢复重放只追加事件与账本，核对当前投影摘要、材料守恒、唯一租约及终局唯一性；发现持久化矛盾时进入只读隔离状态并返回稳定恢复错误码，不猜测或修补业务事实。
3. 外部设备边界采用可脚本化适配器；拒绝、断连、超时和格式错误被持久化为待重试调用，重试次数由逻辑时钟驱动，测试不依赖真实时间或随机退避。
4. 面积率、强度、几何乘加和养护积分封装在检查型整数算术边界内；任何溢出、负间隔、倒序时间或除零使整个命令失败，且不写入中间计算结果。
5. 并发边界依靠数据库唯一约束、版本号和比较交换控制材料池、设备资源、复验源代次、幂等操作号及终局；竞争失败返回可预测错误，查询结果不受goroutine调度顺序影响。
6. 至少设计10个可独立植入且源自同一初始main支线的缺陷点：覆盖重叠漏检、板号竞态、砂浆超领、租约过期仍提交、开放时间边界错误、锚固前缀跳步、定点乘法溢出、异常传播漏项、旧代次回执污染、并发双终局；每个缺陷均由确定性公开测试直接捕获。

## 验收标准

1. 锁定操作完整固化立面几何、基层测区、网格、板材与砂浆批次、洞口翻包、防火隔离带、布胶和锚栓方案、养护、抽检、阈值及任务代次；摘要错配、陈旧规则、缺失或重叠网格必须以稳定有序错误拒绝。
2. 当前有效板代次完整且无非法重叠地覆盖目标网格；整数毫米校验能确定拒绝退化区域、负值、溢出、竖向通缝、洞口角部禁区接缝、错台、锚栓边距不足、孔距不足和孔位干涉。
3. 并发砂浆领取与设备租约获取保持事务原子性和整数克守恒；相同操作号同内容重试返回相同结果，不同内容返回幂等冲突，任何失败后账本与资源状态均与调用前一致。
4. 基层、拌浆、布胶、铺板、板缝、养护、钻孔锚固和检测严格按锁定依赖推进；布胶分区和锚栓序列必须形成完整前缀，错误材料代次、开放时间耗尽、过期租约、越序及重复孔位不得写入。
5. 配水比、单位面积用量、粘结面积率、有效锚固深度、拉拔强度、平整度和养护等效时长均以确定整数定点算法裁定，并覆盖符号、舍入、除零、时间间隔及乘加溢出边界。
6. 五类测量设备发生拒绝、断连、超时或格式错误时，仅形成具有逻辑时间和确定尝试次数的待重试调用；脚本化恢复后才能追加真实读数，故障期间不得推进任务状态。
7. 所有规定异常均按锁定邻接、共享基层测区和同砂浆代次一次性生成确定排序的复验集合；新代次覆盖受影响板片和孔位且保留全部历史，复验代次冲突及旧代次迟到回执不能改变当前结论。
8. 仅在覆盖、守恒、工序前缀、养护、几何、检测、复验和两名不同合格人员复核全部满足时生成唯一抹面移交凭据；移交、质量隔离与取消的同步并发竞争最终恰有一个终局，重启后结果不变。

## 确定性测试场景

1. coverage_geometry_test.go：固定整数网格被板片完整覆盖时锁定成功，并精确断言覆盖摘要。
2. coverage_geometry_test.go：重叠板片、缺失网格与退化多边形分别返回稳定错误码，原因顺序完全一致。
3. coverage_geometry_test.go：竖向通缝和洞口角部禁区接缝被拒绝，而合法错缝方案通过。
4. mortar_lease_test.go：两个goroutine用同步屏障竞争最后一份干粉，只有一个成功且总账守恒。
5. mortar_lease_test.go：材料领取成功但设备租约被占用时，事务回滚后材料余额和租约表均不变化。
6. mortar_lease_test.go：相同操作号同内容重试复用原响应，不同配水量重试返回幂等内容冲突。
7. mortar_lease_test.go：开放截止逻辑时刻前一刻允许铺板，等于或超过截止时按明确边界规则拒绝。
8. workflow_device_test.go：未完成全部布胶分区便铺板、跳过养护便钻孔或跳过锚栓序号均被前缀规则拒绝。
9. workflow_device_test.go：过期钻孔租约、重复孔位、越界孔位及孔位干涉均不产生锚固证据。
10. workflow_device_test.go：脚本化测深仪依次超时、格式错误、成功，前两次只形成待重试记录，第三次才推进状态。
11. workflow_device_test.go：极大面积乘比例及极大拉力除面积触发检查型整数溢出，事务无部分证据。
12. retest_terminal_test.go：拉拔失败按公开邻接表、共享基层测区和同砂浆代次生成去重、有序且唯一的影响集合。
13. retest_terminal_test.go：返工新代次完成后注入旧代次迟到设备回执，当前覆盖和检测结论保持不变。
14. retest_terminal_test.go：养护时间线出现固定缺口时阻止移交并生成复验；补充新代次完整时间线后方可继续。
15. retest_terminal_test.go：同一人员重复复核不满足双人条件，两名不同且资格有效的人员独立通过后满足复核门槛。
16. retest_terminal_test.go：使用同步屏障并发请求移交、隔离和取消，精确断言仅一个成功终局且仅移交胜出时存在凭据。
17. recovery_api_test.go：在材料账本与覆盖事件已提交后重启服务，重建投影与重启前摘要、余额和代次完全相同。
18. recovery_api_test.go：模拟命令中途失败及持久化恢复，断言无部分材料扣减、资源租约、覆盖、锚固证据或终局凭据。
19. recovery_api_test.go：HTTP非法字段、规则摘要陈旧及多项领域错误返回统一JSON结构、稳定状态码和确定原因排序。

## 组件追踪关系

1. 规则锁定、摘要一致性、材料证明及全部定点阈值由“保温构造与材料规则目录”实现，对应验收1和5及coverage_geometry_test.go、recovery_api_test.go。
2. 完整覆盖、网格重叠、错缝、洞口禁区、板代次和邻接查询由“立面铺贴任务及覆盖聚合”实现，对应验收1、2和7及coverage_geometry_test.go、retest_terminal_test.go。
3. 整数克守恒、配水比、开放时间、原子领取、互斥租约和幂等控制由“砂浆守恒与设备租约管理器”实现，对应验收3和4及mortar_lease_test.go。
4. 工序前缀、布胶集合、孔位与深度、养护时间线、设备待重试调用由“布胶铺板锚固及养护证据记录器”实现，对应验收4、5和6及workflow_device_test.go。
5. 抽检、异常传播、返工代次、迟到隔离、双人复核和终局屏障由“拉拔复验与终局仲裁器”实现，对应验收7和8及retest_terminal_test.go。
6. 稳定HTTP契约、事务编排、错误排序、持久化恢复和健康检查由“Go HTTP API”实现，横跨全部验收并由recovery_api_test.go验证。

## 独特性

本计划专注于高层公共建筑薄抹灰岩棉外墙外保温从基层交接到抹面胶浆施工许可的逐板、逐孔质量闭环，以只追加板代次覆盖图、连续布胶覆盖集合、整数克砂浆守恒池和异常影响区复验为核心，不与既有钢构耐火保护、幕墙玻璃装配、植筋或预制构件项目重复。
